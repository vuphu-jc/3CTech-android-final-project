<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khaithu.a3ctech_android_final_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.3CTechandroidfinalproject">
        <activity android:name=".view.VideoPlayerView"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity
            android:name=".view.MovieDetailActivity"
            android:label="@string/title_activity_movie_detail_view"
            android:theme="@style/Theme.3CTechandroidfinalproject.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.3CTechandroidfinalproject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>