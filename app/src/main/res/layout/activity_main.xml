<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".mvp.view.MainActivity">

    <include
        android:id="@+id/include"
        layout="@layout/current_conditions_layout" />

    <include layout="@layout/location_picking_layout" />

    <include android:id="@+id/hourly_forecast_layout"
        layout="@layout/hourly_forecast_layout" />

    <include
        android:id="@+id/bottom_tabs"
        layout="@layout/bottom_tabs_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_400"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/include" />

    <Button
        android:id="@+id/tabsOpenBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tab_bottom_btn_text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/alarmSettingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:text="Alarm"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
